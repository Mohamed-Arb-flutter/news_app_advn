import 'package:flutter/material.dart';

class SearchRuslt extends StatelessWidget {
  const SearchRuslt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Search results'),centerTitle: true,),body: Column(children: [],),);
  }
}